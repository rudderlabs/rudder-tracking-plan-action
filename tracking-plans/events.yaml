version: rudder/0.1
kind: events
metadata:
  name: myeventgroup
spec:
  events:
    - id: product_viewed
      display_name: "Product Viewed"
      event_type: track
      description: "This event is triggered every time a user views a product."

